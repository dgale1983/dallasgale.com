import React from 'react'

// import PropTypes from 'prop-types';

const TextBlock = ({ children }) => {
  return <div className="typography__block">{children}</div>
}

// TextBlock.propTypes = {

// };

export default TextBlock
